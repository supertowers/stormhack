<%= form_for(@site) do |f| %>
  <% if @site.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@site.errors.count, "error") %> prohibited this site from being saved:</h2>

      <ul>
      <% @site.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :url %><br>
    <%= f.text_field :url, class: 'form-control parsley-validated' %>
  </div>
  <div class="form-group">
    <%= f.label :public_description %><br>
    <%= f.text_area :public_description, class: 'form-control parsley-validated' %>
  </div>
  <div class="">
  </div>
  <div class="form-group">
    <%= f.label :private_description %><br>
    <%= f.text_area :private_description, class: 'form-control parsley-validated' %>
  </div>
  <div class="form-group">
    <%= f.label :screenshot %><br>
    <%= f.file_field :screenshot, class: 'form-control parsley-validated'  %>
  </div>
  <div class="site-actions">
    <%= f.submit 'Guardar', class: 'btn btn-md btn-primary' %>
  </div>
<% end %>
